# db configuration
db.type=mysql
db.driver=com.mysql.cj.jdbc.Driver
db.url=jdbc:mysql://127.0.0.1:3306/test?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowMultiQueries=true
db.username=root
db.password=root

# other
author=Destiny
project.name=springboot-generator

# let it empty to using default configuration, it will generate to this project
file.output.path=
# package path
file.package.path=com.destiny.generator

# --- table configurations ---
# use to remove prefix
table.prefix.list=xtl_,
# the tables need to generate
table.name.list=xtl_admin_user,

# swagger config
swagger.projectName=代码生成项目
swagger.version=1.0.0
swagger.description=用于快速搭建项目